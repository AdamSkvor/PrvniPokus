using UnityEngine;

public class Bachova : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("ahoj, zdravím vsechny");
    }

    // Update is called once per frame
    void Update()
    {
        //Sem nepatří pozdrav
    }
}
