using UnityEngine;

public class Hajkova : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
void Start()
    {
   //změna
    }

    // Update is called once per frame
    void Update()
    {
        //Sem nepatří pozdrav
    }
}
