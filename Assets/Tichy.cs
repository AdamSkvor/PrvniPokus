using UnityEngine;

public class Tichy : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
void Start()
    {
       Debug.Log("Ahoj, jsem alex a jsem hlučný, ale to nevadí, protože jsem tichy"); 
        //Sem patří pozdrav
        //Sem patří pozdrav
        //Sem patří pozdrav
    }

    // Update is called once per frame
    void Update()
    {
        //Sem nepatří pozdrav
    }
}
